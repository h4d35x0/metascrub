org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# NOTE: org.gradle.java.home is deliberately NOT set here. It would be a machine
# path in a committed file. Pass the JDK on the command line instead:
#   gradlew.bat -Dorg.gradle.java.home="<path to a JDK 17>" assembleDebug
